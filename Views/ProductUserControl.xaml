<UserControl x:Class="POS_system_myowndesign.Views.ProductUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS_system_myowndesign.Views"
             mc:Ignorable="d" 
             d:DesignHeight="75" d:DesignWidth="75" Margin="5">
    <Button Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
            CommandParameter="{Binding}"
            BorderThickness="1"
            BorderBrush="Gray"
            Background="White"
            Padding="5">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Bind directly to the current product -->
            <TextBlock Text="{Binding Name}" 
                       FontWeight="Bold" 
                       FontSize="14" 
                       TextAlignment="Center"/>
            <TextBlock Text="{Binding Price, StringFormat=C}" 
                       Foreground="Green" 
                       FontSize="12" 
                       TextAlignment="Center"/>
        </StackPanel>
    </Button>
</UserControl>
