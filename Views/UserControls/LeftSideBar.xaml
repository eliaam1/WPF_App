<UserControl x:Class="POS_system_myowndesign.Views.UserControls.LeftSideBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS_system_myowndesign.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="644" d:DesignWidth="160">
    <UserControl.Resources>
        <Style x:Key="SearchButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="47"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#5d5e5c"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style x:Key="ActiveButtonStyle" TargetType="Button" BasedOn="{StaticResource SearchButtonStyle}">
            <Setter Property="Background" Value="#2496ff"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Grid.Column="0" Grid.Row="1" Background="#F5F5F5">
            <Button Style="{StaticResource SearchButtonStyle}" Content="Home" x:Name="HomeBtn" Click="HomeBtn_Click"/>
            <Button Style="{StaticResource SearchButtonStyle}" Content="Tables" x:Name="TablesBtn" Click="TablesBtn_Click"/>
            <Button Style="{StaticResource SearchButtonStyle}" Content="Orders" x:Name="OrdersBtn" Click="OrdersBtn_Click"/>
            <Button Style="{StaticResource SearchButtonStyle}" Content="Reports" x:Name="ReportsBtn" Click="ReportsBtn_Click"/>
            <Button Style="{StaticResource SearchButtonStyle}" Content="Settings"/>
            <StackPanel Margin="0,270,0,0">
                <Button Style="{StaticResource SearchButtonStyle}" Content="➜]Logout" x:Name="logoutBtn" Click="logoutBtn_Click"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
