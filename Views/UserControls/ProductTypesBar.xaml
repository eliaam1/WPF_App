<UserControl x:Class="POS_system_myowndesign.Views.UserControls.ProductTypesBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS_system_myowndesign.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="540">
    <UserControl.Resources>
        <Style x:Key="HeaderButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="90"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#4d4d5c"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <!-- Starters Button -->
            <Button Command="{Binding FilterByTypeCommand}" 
                    CommandParameter="Starters"
                    Content="Starters">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource HeaderButtonStyle}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedType}" Value="Starters">
                                <Setter Property="Background" Value="#FF4CAF50"/>
                                <Setter Property="Foreground" Value="White"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Breakfast Button -->
            <Button Command="{Binding FilterByTypeCommand}" 
                    CommandParameter="Breakfast"
                    Content="Breakfast">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource HeaderButtonStyle}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedType}" Value="Breakfast">
                                <Setter Property="Background" Value="#FF4CAF50"/>
                                <Setter Property="Foreground" Value="White"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Lunch Button -->
            <Button Command="{Binding FilterByTypeCommand}" 
                    CommandParameter="Lunch"
                    Content="Lunch">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource HeaderButtonStyle}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedType}" Value="Lunch">
                                <Setter Property="Background" Value="#FF4CAF50"/>
                                <Setter Property="Foreground" Value="White"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Supper Button -->
            <Button Command="{Binding FilterByTypeCommand}" 
                    CommandParameter="Supper"
                    Content="Supper">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource HeaderButtonStyle}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedType}" Value="Supper">
                                <Setter Property="Background" Value="#FF4CAF50"/>
                                <Setter Property="Foreground" Value="White"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Desserts Button -->
            <Button Command="{Binding FilterByTypeCommand}" 
                    CommandParameter="Desserts"
                    Content="Desserts">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource HeaderButtonStyle}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedType}" Value="Desserts">
                                <Setter Property="Background" Value="#FF4CAF50"/>
                                <Setter Property="Foreground" Value="White"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Beverages Button -->
            <Button Command="{Binding FilterByTypeCommand}" 
                    CommandParameter="Beverages"
                    Content="Beverages">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource HeaderButtonStyle}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedType}" Value="Beverages">
                                <Setter Property="Background" Value="#FF4CAF50"/>
                                <Setter Property="Foreground" Value="White"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
