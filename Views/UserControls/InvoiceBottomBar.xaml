<UserControl x:Class="POS_system_myowndesign.Views.UserControls.InvoiceBottomBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS_system_myowndesign.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="238" d:DesignWidth="319">
    <Grid>
        <Border BorderBrush="LightGray" BorderThickness="0,1,0,0" Padding="15">
            <StackPanel>
                <Grid Margin="0,5,0,5">
                    <TextBlock Text="SubTotal" FontSize="16"/>
                    <TextBlock Text="{Binding SubTotal, StringFormat=C}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Right"/>
                </Grid>
                <Grid Margin="0,5,0,5">
                    <TextBlock Text="Discount (%)" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding DiscountPercentage, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="60" HorizontalAlignment="Right" TextAlignment="Center"/>
                </Grid>
                <Grid Margin="0,5,0,5">
                    <TextBlock Text="Tax" FontSize="16"/>
                    <TextBlock Text="{Binding Tax, StringFormat=C}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Right"/>
                </Grid>
                <Separator Margin="0,10"/>
                <Grid Margin="0,5,0,5">
                    <TextBlock Text="Payable Amount" FontSize="20" FontWeight="ExtraBold"/>
                    <TextBlock Text="{Binding TotalPayable, StringFormat=C}" FontSize="20" FontWeight="ExtraBold" HorizontalAlignment="Right"/>
                </Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button Content="Clear" Width="120" Height="44" FontSize="20" Margin="0,0,10,0"/>
                    <Button Content="Complete" Width="120" Height="44" FontSize="20" FontWeight="Bold" 
                Command="{Binding CompleteOrderCommand}"
                Background="SeaGreen" Foreground="White"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
