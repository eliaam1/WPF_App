<UserControl x:Class="POS_system_myowndesign.Views.UserControls.RightSideBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS_system_myowndesign.Views.UserControls"
             xmlns:viewmodels="clr-namespace:POS_system_myowndesign.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="379" d:DesignWidth="310">

    <ListView ItemsSource="{Binding Cart}" Margin="5" Background="Transparent" BorderThickness="0">
        <ListView.ItemTemplate>
            <DataTemplate>
                <Border Width="Auto" MinWidth="280" BorderBrush="LightGray" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Product.Name}" VerticalAlignment="Center" FontSize="14"/>
                        <TextBlock Text="{Binding Quantity, StringFormat='x{0}'}" Grid.Column="1" Margin="10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding TotalPrice, StringFormat=C}" Grid.Column="2" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button Content="-" Grid.Column="3" Width="25" Height="25" Margin="5,0" 
                                    Command="{Binding DataContext.DecrementItemCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                    CommandParameter="{Binding}"
                                    Background="IndianRed" Foreground="White" BorderThickness="0"/>
                    </Grid>
                </Border>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    

    
</UserControl>
