<UserControl x:Class="POS_system_myowndesign.Views.UserControls.TopHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS_system_myowndesign.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="960">
    <Grid>
        <Grid Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Restaurant POS" FontFamily="fantasy" FontSize="30" FontWeight="ExtraBold"
               VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="20,0,0,0">
                <TextBox Text="Search for Products..." Foreground="LightGray" VerticalAlignment="Center" 
                 Width="250" Height="40" FontSize="20" VerticalContentAlignment="Center"/>
                <Button Width="50" Height="40" Content="🔍" FontSize="23" Foreground="Gray" Background="White" BorderThickness="0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
