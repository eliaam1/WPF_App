<UserControl x:Class="POS_system_myowndesign.Views.UserControls.TablesUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS_system_myowndesign.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="604" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Table Management Controls -->
        <StackPanel Orientation="Horizontal" Margin="5">
            <Button Content="Add Table" Command="{Binding AddTableCommand}" Margin="5" Padding="10,5"/>
            <Button Content="Edit Table" Command="{Binding EditTableCommand}" Margin="5" Padding="10,5"/>
            <Button Content="Delete Table" Command="{Binding DeleteTableCommand}" Margin="5" Padding="10,5"/>
        </StackPanel>

        <!-- Tables List -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Tables}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.SelectTableCommand, 
                                    RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding}"
                            Margin="10" Width="100" Height="100">
                        <TextBlock Text="{Binding TableNumber}" FontSize="20" FontWeight="Bold"/>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
